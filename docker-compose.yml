version: '3.8'

services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: prep
      POSTGRES_USER: prep
      POSTGRES_PASSWORD: prep
      PGDATA: /data/postgres
    ports:
      - "5432:5432"
